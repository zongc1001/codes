/**
 * @param {Array} matrix
*/
function printMatrix(matrix)
{
    let m = 0, n = 0;
    let dirX = [1, 0, -1, 0];
    let dirY = [0, 1, 0, -1];
    let len = matrix.length;
    if(len === 1) {
        console.log(matrix[0][0]);
    } else{
        
    }
}