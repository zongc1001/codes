#include <iostream>
#include <set>

using namespace std;

int n;
set<int> st;

int main(int argc, char const *argv[])
{
  cin >> n;
  for (int i = 0; i < n; i++)
  {
    int temp;
    cin >> temp;
    st.find(temp);
    


  }

  return 0;
}
